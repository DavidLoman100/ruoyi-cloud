<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.infrastructure.menu.repository.mapper.SysMenuMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.system.infrastructure.menu.repository.po.SysMenuPo">
            <id property="menuId" column="menu_id" jdbcType="BIGINT"/>
            <result property="menuName" column="menu_name" jdbcType="VARCHAR"/>
            <result property="parentId" column="parent_id" jdbcType="BIGINT"/>
            <result property="orderNum" column="order_num" jdbcType="INTEGER"/>
            <result property="path" column="path" jdbcType="VARCHAR"/>
            <result property="component" column="component" jdbcType="VARCHAR"/>
            <result property="query" column="query" jdbcType="VARCHAR"/>
            <result property="routeName" column="route_name" jdbcType="VARCHAR"/>
            <result property="isFrame" column="is_frame" jdbcType="INTEGER"/>
            <result property="isCache" column="is_cache" jdbcType="INTEGER"/>
            <result property="menuType" column="menu_type" jdbcType="CHAR"/>
            <result property="visible" column="visible" jdbcType="CHAR"/>
            <result property="status" column="status" jdbcType="CHAR"/>
            <result property="perms" column="perms" jdbcType="VARCHAR"/>
            <result property="icon" column="icon" jdbcType="VARCHAR"/>
            <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        menu_id,menu_name,parent_id,
        order_num,path,component,
        query,route_name,is_frame,
        is_cache,menu_type,visible,
        status,perms,icon,
        create_by,create_time,update_by,
        update_time,remark
    </sql>

    <select id="qryMenuByPermission" resultType="com.ruoyi.system.infrastructure.menu.repository.po.SysMenuPo">
        select m.menu_id,
        m.parent_id,
        m.menu_name,
        m.path,
        m.component,
        m.query,
        m.route_name,
        m.visible,
        m.status,
        m.perms,
        m.is_frame,
        m.is_cache,
        m.menu_type,
        m.icon,
        m.order_num,
        m.create_time
        from sys_menu m
        left join sys_role_menu rm on m.menu_id = rm.menu_id
        where rm.role_id in
        <foreach collection="qry.roles" item="role" open="(" separator="," close=")">
            #{role}
        </foreach>
        <if test="qry.menuName != null and qry.menuName != ''">
            AND m.menu_name like concat('%', #{qry.menuName}, '%')
        </if>
        <if test="qry.visible != null and qry.visible != ''">
            AND m.visible = #{qry.visible}
        </if>
        <if test="qry.status != null and qry.status != ''">
            AND m.status = #{qry.status}
        </if>
        order by m.parent_id, m.order_num
    </select>

    <select id="getMenuIdByRole" resultType="java.lang.Long">
        select m.menu_id
        from sys_menu m
        left join sys_role_menu rm on m.menu_id = rm.menu_id
        where rm.role_id = #{roleId}
        <if test="menuCheckStrictly != 0">
            and m.menu_id not in (select m.parent_id from sys_menu m inner join sys_role_menu rm on m.menu_id = rm.menu_id and rm.role_id = #{roleId})
        </if>
        order by m.parent_id, m.order_num
    </select>
</mapper>
