<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.infrastructure.role.repository.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.system.infrastructure.role.repository.po.SysRolePo">
            <id property="roleId" column="role_id" jdbcType="BIGINT"/>
            <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
            <result property="roleKey" column="role_key" jdbcType="VARCHAR"/>
            <result property="roleSort" column="role_sort" jdbcType="INTEGER"/>
            <result property="dataScope" column="data_scope" jdbcType="CHAR"/>
            <result property="menuCheckStrictly" column="menu_check_strictly" jdbcType="TINYINT"/>
            <result property="deptCheckStrictly" column="dept_check_strictly" jdbcType="TINYINT"/>
            <result property="status" column="status" jdbcType="CHAR"/>
            <result property="isDeleted" column="is_deleted" jdbcType="CHAR"/>
            <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        role_id,role_name,role_key,
        role_sort,data_scope,menu_check_strictly,
        dept_check_strictly,status,is_deleted,
        create_by,create_time,update_by,
        update_time,remark
    </sql>
    <select id="pageQryRoleListByPerms"
            resultType="com.ruoyi.system.infrastructure.role.repository.po.SysRolePo">
        select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.data_scope, r.menu_check_strictly, r.dept_check_strictly,
                        r.status, r.is_deleted, r.create_time, r.remark
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.role_id
                 left join sys_user u on u.user_id = ur.user_id
                 left join sys_dept d on u.dept_id = d.dept_id
        where r.is_deleted = '0'
            and u.is_deleted = '0'
            and d.is_deleted = '0'
<!--        <if test="qry.roleId != null and qry.roleId != 0">-->
<!--            AND r.role_id = #{qry.roleId}-->
<!--        </if>-->
        <if test="qry.roleName != null and qry.roleName != ''">
            AND r.role_name like concat('%', #{qry.roleName}, '%')
        </if>
        <if test="qry.status != null and qry.status != ''">
            AND r.status = #{qry.status}
        </if>
        <if test="qry.roleKey != null and qry.roleKey != ''">
            AND r.role_key like concat('%', #{qry.roleKey}, '%')
        </if>
        <if test="qry.startCreateTime != null and qry.startCreateTime != ''"><!-- 开始时间检索 -->
            and date_format(r.create_time,'%Y%m%d') &gt;= date_format(#{qry.startCreateTime},'%Y%m%d')
        </if>
        <if test="qry.endCreateTime != null and qry.endCreateTime != ''"><!-- 结束时间检索 -->
            and date_format(r.create_time,'%Y%m%d') &lt;= date_format(#{qry.endCreateTime},'%Y%m%d')
        </if>
        <!-- 数据范围过滤 -->
        ${qry.dataScopeSql}
        order by r.role_sort
    </select>
    <select id="getRoleByPerms" resultType="com.ruoyi.system.infrastructure.role.repository.po.SysRolePo">
        select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.data_scope, r.menu_check_strictly, r.dept_check_strictly,
        r.status, r.is_deleted, r.create_time, r.remark
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        left join sys_user u on u.user_id = ur.user_id
        left join sys_dept d on u.dept_id = d.dept_id
        where r.is_deleted = '0'
        and u.is_deleted = '0'
        and d.is_deleted = '0'
        and r.role_id = #{roleId}
        ${dataScopeSql}
    </select>

</mapper>
